def area_tri(base, altura):
    return base * altura / 2.

base = float( input("Entre com a base do triangulo: ") )
altura = float( input("Entre com a altura do triangulo: ") )

print( area_tri( base, altura) )
